package gui.main;

import javax.swing.JFrame;

public interface AddMenu {
	void top(JFrame frame);
	void middle(JFrame frame);
	void middle2(JFrame frame);
	void bottom(JFrame frame);
	void timeTable(JFrame frame);
}
